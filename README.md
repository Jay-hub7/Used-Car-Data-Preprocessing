# Used-Car-Data-Preprocessing

Preprocessed the Used Car Resale dataset by checking missing values, duplicates, and outliers. Applied IQR-based outlier handling, categorical encoding, feature scaling, and train-test splitting while preventing data leakage.

The final preprocessed dataset was exported and is ready for machine learning.
